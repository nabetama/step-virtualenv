box: wercker/default
build:
  steps:
    - validate-wercker-step
    - script:
        name: tree
        code: ls -la
    - script:
        name: Run tests
        code: ./script/spec
  after-steps:
    - script:
        name: Save stats
        code: ./run.sh